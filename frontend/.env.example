# API base path the SPA calls (proxied in dev, same-origin in prod behind a gateway).
VITE_API_BASE_URL=/api/v1

# Dev-only: where the Vite dev-server proxies /api to (the FastAPI backend).
VITE_PROXY_TARGET=http://localhost:8000
